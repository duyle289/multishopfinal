@using WebBanlaptop.Models;
@model NHASANXUAT

@{
    ViewBag.Title = "SuaNSX";
    Layout = "~/Views/Layout/_AdminLayout.cshtml";
    NHASANXUAT nsx = ViewBag.nsx;
}

<div class="card shadow mb-4">
    <div class="card-body">
        <div class="table-responsive">
            <div id="dataTable_wrapper" class="dataTables_wrapper dt-bootstrap4">
                <div class="col-sm-12">
                    @using (Html.BeginForm("SuaNSX", "Admin", FormMethod.Post, new { @enctype = "multipart/form-data" }))
                    {
                        @Html.AntiForgeryToken()

                        <div class="form-horizontal">
                            <h1 class="h3 mb-2 text-gray-800">Cập nhật nhà sản xuất</h1>
                            <hr />
                            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                            @Html.HiddenFor(model => model.MANSX)

                            <div class="form-group">
                                @Html.Label("", "Tên nhà sản xuất", htmlAttributes: new { @class = "control-label col-md-2" })
                                <div class="col-md-10">
                                    @Html.TextBox("TENNSX", nsx.TENNSX, new { @class = "form-control" })

                                </div>
                            </div>

                            <div class="form-group">
                                @Html.Label("", "Trạng thái", htmlAttributes: new { @class = "control-label col-md-2" })
                                <div class="col-md-10">
                                    <div class="checkbox">
                                        @Html.CheckBox("TRANGTHAI", nsx.TRANGTHAI, new { htmlAttributes = new { @class = "form-control" } })

                                    </div>
                                </div>
                            </div>

                            <div class="form-group">
                                @Html.Label("", "Icon", htmlAttributes: new { @class = "control-label col-md-2" })
                                <div class="col-md-10">
                                    <input name="Icon" type="file" />
                                    <span style="color:red">@ViewBag.upload</span>
                                    <br />
                                </div>
                                <img src="@Url.Content("~/Content/logoSP/" + nsx.Icon)" width="300" />
                            </div>

                            <div class="form-group">
                                <div class="col-md-offset-2 col-md-10">
                                    <input type="submit" value="Lưu" class="btn btn-default btn btn-info create-admin" />
                                    <a href="@Url.Action("ListNSX", "Admin")" class="btn btn-danger">Trở lại</a>
                                </div>
                            </div>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>



